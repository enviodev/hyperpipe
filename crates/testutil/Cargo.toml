[package]
name = "hp-testutil"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Test-only helpers (scripted HTTP server). Never a runtime dependency."
publish = false

[dependencies]
serde_json = { workspace = true }
tokio = { workspace = true }
